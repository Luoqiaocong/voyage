# 统一换行风格为 LF。
#
# 为什么需要这个文件：仓库里所有文件都是 LF，但 Windows 上 git 默认
# core.autocrlf=true，会在提交时把工作区的 LF 改写成 CRLF，
# 导致每次新增文件都报「LF will be replaced by CRLF」并可能被拒绝提交。
# 显式声明后，换行风格由仓库决定，不再随各人的本地配置变化。
#
# 影响面：只规范文本文件；图片等二进制文件不受影响。
* text=auto eol=lf

# 二进制文件不做换行转换，避免被破坏
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.gif binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary

# Windows 批处理必须用 CRLF，否则可能无法执行
*.bat text eol=crlf
*.cmd text eol=crlf
