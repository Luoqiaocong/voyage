# Vue 前端环境变量
#
# 用法：复制为 .env 后使用（.env 已被 .gitignore 忽略，不会入库）。
#
# ⚠️ 本地开发**必须**配这一项。留空或缺省时前端会回落到相对路径 /api/v1，
# 而 Vite dev server 没有配置 proxy，该路径会被它的 SPA 回退当成前端路由、
# 直接返回 index.html —— 症状是「所有接口都返回 200，但页面没有数据」，
# 排查时容易怀疑后端，实际请求根本没到后端。
#
# 生产部署（Docker）不需要这个文件：nginx 会把 /api 反向代理到后端。
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

